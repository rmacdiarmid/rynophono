Quick go program to retrieve phone numbers and website from google places API

Requires API key, runs as a go test by reading in testdata infile and appending results to the csv

export GAPIKEY="{{KEY}}"
go test

